'use client';

import styles from '../styles/components/Loader2.module.css'; // Import the CSS module for styling

const Loader = () => {
  return (
    <div className={styles.loading}>
    <span></span>
    <span></span>
    <span></span>


    </div>
  );
};

export default Loader;
